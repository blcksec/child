#include "child/language/operator.h"

namespace Child {
    namespace Language {
        CHILD_DEFINITION(Operator, Object, Language);

        void Operator::initRoot() {
            // TODO
        }
    }
}
