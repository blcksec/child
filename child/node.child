Node {
doc:
    "Base element grandfather of everything."
body:
    \fork {
    doc:
        "Fork the receiver."
    test:
        ?: true
    }
test:
    // QVERIFY(!Node::root()->origin());
    // QVERIFY(Node::root()->hasDirectParent(Node::root()));
    ?: 1 == 1
}
