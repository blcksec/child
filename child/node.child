Node {
doc:
    "Root element, grandfather of everything."
body:
    \origin {
    test:
        n1 := Node()
        n2 := Node()
        ?: n2 origin == Node
        n2 origin = n1
        ?: n2 origin == n1
    }

    \fork {
    doc:
        "Fork the receiver."
    }
test:
    ?: origin == self
    ?: parent == self
}
